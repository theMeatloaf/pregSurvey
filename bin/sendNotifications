#!/usr/bin/env node

const test = require('dotenv').config()
const request = require('request');

  var pgp = require('pg-promise')();
  var connectionString = process.env.DATABASE_URL;
  db = pgp(connectionString);

 var API_URL = "https://api:"+process.env.MAILGUN_API_KEY+"@api.mailgun.net/v3/" + process.env.MAILGUN_DOMAIN + "/messages";

  request.post(API_URL,
    { form: { from: 'test@test.de', to: 'ryan@thereceptionist.com', subject:'heyo maggots', text:'HEYOOO MAGOTS'  } },
    function (error, response, body) {
        if (!error && response.statusCode == 200) {
          console.error('error free');
        	done();
        } else {
           console.error(response);
        }
    }
  );